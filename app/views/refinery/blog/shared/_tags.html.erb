<div class="widget clearfix tags">
  <h2><%= t('.title') %></h2>
  <nav>
    <% tag_cloud(@tags, %w(tag1 tag2 tag3 tag4)) do |tag, css_class| %>
      <%= link_to tag.name, refinery.tagged_posts_path(tag), class: css_class %>
    <% end %>
  </nav>
</div>
