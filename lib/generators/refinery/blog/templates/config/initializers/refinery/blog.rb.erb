# encoding: utf-8
Refinery::Blog.configure do |config|
  # config.per_page = <%= Refinery::Blog.per_page.inspect %>

  # Configure how many records (posts/categories) per page should be displayed
  # in the list of records in the Blog admin area
  # config.per_admin_page = <%= Refinery::Blog.per_admin_page.inspect %>

  # Supported: <%= Refinery::Blog.sharing_services.inspect %>
  # config.sharing_service = <%= Refinery::Blog.sharing_service.inspect %>

  # Supported: <%= Refinery::Blog.comment_services.inspect %>
  # config.comment_service = <%= Refinery::Blog.comment_service.inspect %>
end
